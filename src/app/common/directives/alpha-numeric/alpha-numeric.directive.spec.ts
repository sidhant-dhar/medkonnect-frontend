import { AlphaNumericDirective } from './alpha-numeric.directive';

describe('AlphaNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphaNumericDirective(null);
    expect(directive).toBeTruthy();
  });
});
